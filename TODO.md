.trae/documents/node-password-encryption-plan.md
docs/design/PLAYBOOK_TEMPLATE_SYSTEM.md
.trae/specs/add-ai-input-completion
增加owl-serve的日志流程
owl serve和owl-serve的参数保持一致，后者同步前者，以及补全前者
AI对话要升级Markdown格式展示和html的一些组件，比如<a>链接，和文件下载，和选择器，不过先分析是否会引入大量依赖导致编译文件体积变大
节点分组变丑了，没左对齐
AI对话的对话上下文改为固定的话术列表，点击即可在指令里输入
右增加一个列表，即话术列表，用于方便用户点击后，直接在输入框里填充，左边则是新增会话，会话列表，其中每条会话展示简单标题（AI总结第一条语句生成标题），以及时间，鼠标移动上去，右侧显示可删除的按钮，以及会话卡片加深颜色
